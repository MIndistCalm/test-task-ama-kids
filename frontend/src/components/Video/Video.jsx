import React from 'react';

import Display from '../../styles/Video/Display.module.css';
const Video = () => {
    return (
        <div className={Display.display}>
            d
        </div>
    );
}

export default Video;
